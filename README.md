Experiment with React, mobx, webpack, facebook sso

# install & run

- npm install
- rename assets/js/config.json.example into assets/js/config.json and put a valid appId (facebook app)
- npm start
- visit localhost:8080
